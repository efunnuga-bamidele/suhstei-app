export const selectMyBooksMap = (state) => {
    // console.log(state['mybooks'])
    // state.filter(docSnapshot => { if(docSnapshot.id === "dTARGAWUzGb1cym3h0mA61EcdGy1") {
    //     // return docSnapshot.data()
    //     console.log(docSnapshot.data())
    // } });
    // return state['mybooks'].reduce((acc, books) => {
    //     // console.log(book)
    //     const { index, mybook } = books;
    //     acc[index] = mybook
    //     console.log(acc)
    //     // return mybooks
    // }, {})
}